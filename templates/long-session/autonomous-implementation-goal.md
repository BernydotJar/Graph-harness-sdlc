# Autonomous Long-Session Implementation Goal Template

Use this template to run a bounded, spec-driven implementation session that can continue autonomously while useful, verifiable work remains.

This template does not replace the harness lifecycle. Before execution, the target feature must exist in the target repository, have an approved specification, define file boundaries, and identify verification commands.

```text
/goal