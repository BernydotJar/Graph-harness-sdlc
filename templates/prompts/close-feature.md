# Prompt: close-feature

Close a reviewed feature only after:

- review passed
- verification passed
- human closure approval is explicit

Move the feature from `review` to `done`, update progress, and run `./init.sh`.

