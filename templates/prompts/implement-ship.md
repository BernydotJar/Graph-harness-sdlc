# Prompt: implement-ship

Implement an approved SHIP feature.

Follow the approved spec and apply production criteria while implementing.

Stop for explicit approval before dependencies, schema changes, migrations, destructive commands, or scope changes.

