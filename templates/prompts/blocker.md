# Prompt: blocker

Use this when work cannot continue safely.

Record:

- feature id
- current status
- blocker
- owner of next decision
- smallest safe next action

Set status to `blocked` only when progress is impossible without human input or external state.

