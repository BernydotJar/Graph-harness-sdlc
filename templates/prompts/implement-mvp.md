# Prompt: implement-mvp

Implement an approved MVP feature.

Follow the approved spec, touch only allowed files, run required verification, and move the feature to `review`.

Do not add production-hardening scope unless the spec requires it.

